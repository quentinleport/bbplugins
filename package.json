{
  "name": "blockbench-plugins",
  "version": "1.0.0",
  "description": "Blockbench plugins development repository",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --outfile=dist/center_object_plugin.js",
    "dev": "esbuild src/plugin.ts --bundle --outfile=dist/center_object_plugin.js --watch"
  },
  "author": "Quentin Le Port",
  "license": "MIT",
   "dependencies": {
    "blockbench-types": "5.0.6"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
